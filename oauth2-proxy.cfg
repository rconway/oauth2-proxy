provider = "github"
# client_id = "xxx"
# client_secret = "xxx"
# cookie_secret = "xxx"
redirect_url = "http://localhost:4180/oauth2/callback"
email_domains = [ "*" ]  # Use "*" to allow any GitHub user or specify allowed domains
upstreams = [ "http://localhost:8000/" ]
# github_org = "your-github-org"  # Optional: restrict to members of a GitHub organization
# github_team = "your-github-team"  # Optional: restrict to members of a specific team within an org
